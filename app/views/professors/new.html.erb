
<%= form_with scope: :professor, url: professors_path, local: true do |form| %>

  <% if @professor.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@professor.errors.count, "error") %> prohibited
      this professor record from being saved:
    </h2>
    <ul>
      <% @professor.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<p class="creation-guide">Professor's status can be found on each professor's profile page in each department's website</p>


<div class="actions">
<%= form_for @professor, html: { multipart: true } do |f| %>
  <h2>Add a professor</h2>
  <%= form.text_field :name, placeholder: "Enter professor's name" %> 
  <%= form.text_field :department, placeholder: "Enter professor's department" %> 
  <%= form.text_field :position, placeholder: "Enter professor's status" %> 
  <%= form.submit class: 'newUserBtn' %>


<% end %>
</div>


  <% end %>