
<%= form_with scope: :unimodule, url: unimodules_path, local: true do |form| %>

    <% if @unimodule.errors.any? %>
    <div id="error_explanation">
      <h2>
        <%= pluralize(@unimodule.errors.count, "error") %> prohibited
        this module record from being saved:
      </h2>
      <ul>
        <% @unimodule.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  
  <p class="creation-guide">Module's status can be found on each professor's profile page in each department's website</p>
  
  
  <div class="actions">
  <%= form_for @unimodule, html: { multipart: true } do |f| %>
    <h2>Add a unimodule</h2>
    <%= form.text_field :code, placeholder: "code" %> 
    <%= form.text_field :title, placeholder: "title" %> 
    <%= form.submit class: 'newUserBtn' %>
  
  
  <% end %>
  </div>
  
  
    <% end %>